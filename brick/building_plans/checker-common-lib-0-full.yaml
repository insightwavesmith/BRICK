plan_ref: building-plan:checker-common-lib-0-full
owner_axis: Brick
building_id: checker-common-lib-0-0526
selected_adapter_ref: adapter:local
proof_limits:
  - support evidence only
  - not source truth
  - not success judgment
  - not quality judgment
  - not Movement authority
not_proven:
  - full checker deletion readiness
  - all 139 checkers migrated
  - fixture deduplication completeness
steps:
  - step_ref: checker-common-lib-0-01-inventory
    selected_adapter_ref: adapter:local
    rows:
      - axis: Brick
        row_ref: brick-row:ccl0-inventory
        brick_work_ref: work:ccl0-inventory
        brick_instance_ref: brick-ccl0-inventory
        work_statement: Inventory repeated checker helper patterns and define the smallest common support library surface.
        comparison_rule: Inventory must distinguish common support mechanics from checker authority and must not delete checkers.
        required_return_shape: inventory_counts, repeated_patterns, chosen_common_surface, proof_limits, not_proven
        source_facts:
          - support/checkers/check_building_automation_level_next0.py
          - support/checkers/check_building_automation_final_completion0.py
          - support/checkers/check_multi_llm_final_qa0.py
      - axis: Agent
        row_ref: agent-row:ccl0-inventory
        agent_object_ref: agent-object:coo
      - axis: Link
        row_ref: link-row:ccl0-inventory-to-development
        movement: forward
        target_ref: brick-ccl0-development
        next_brick_instance_ref: brick-ccl0-development
  - step_ref: checker-common-lib-0-02-development
    selected_adapter_ref: adapter:local
    rows:
      - axis: Brick
        row_ref: brick-row:ccl0-development
        brick_work_ref: work:ccl0-development
        brick_instance_ref: brick-ccl0-development
        work_statement: Add support/checkers/lib common helpers and migrate representative active automation checkers to use them.
        comparison_rule: Development must not change checker proof meaning, source truth boundary, or Movement authority boundary.
        required_return_shape: changed_surfaces, migrated_checkers, unchanged_boundaries, proof_limits, not_proven
        source_facts:
          - AGENTS.md
          - support/checkers/check_package_path_admission.py
      - axis: Agent
        row_ref: agent-row:ccl0-development
        agent_object_ref: agent-object:dev
      - axis: Link
        row_ref: link-row:ccl0-development-to-qa
        movement: forward
        target_ref: brick-ccl0-qa
        next_brick_instance_ref: brick-ccl0-qa
  - step_ref: checker-common-lib-0-03-qa
    selected_adapter_ref: adapter:local
    rows:
      - axis: Brick
        row_ref: brick-row:ccl0-qa
        brick_work_ref: work:ccl0-qa
        brick_instance_ref: brick-ccl0-qa
        work_statement: Verify CHECKER-COMMON-LIB-0 preserves active checker behavior and package path boundaries.
        comparison_rule: QA must report checker output evidence only and must not claim full deletion readiness.
        required_return_shape: checker_observations, migrated_behavior, remaining_cleanup, proof_limits, not_proven
        source_facts:
          - support/checkers/lib/common.py
          - support/checkers/profiles/core.yaml
      - axis: Agent
        row_ref: agent-row:ccl0-qa
        agent_object_ref: agent-object:qa
      - axis: Link
        row_ref: link-row:ccl0-qa-to-closure
        movement: forward
        target_ref: brick-ccl0-closure
        next_brick_instance_ref: brick-ccl0-closure
  - step_ref: checker-common-lib-0-04-closure
    selected_adapter_ref: adapter:local
    rows:
      - axis: Brick
        row_ref: brick-row:ccl0-closure
        brick_work_ref: work:ccl0-closure
        brick_instance_ref: brick-ccl0-closure
        work_statement: Close CHECKER-COMMON-LIB-0 and name the next compaction slice.
        comparison_rule: Closure may identify next checker thinning work but must not claim the checker set is already compacted.
        required_return_shape: observed_evidence, narrowly_proven, not_proven, held, next_movement_candidate
        source_facts:
          - brick/building_plans/checker-common-lib-0-full.yaml
      - axis: Agent
        row_ref: agent-row:ccl0-closure
        agent_object_ref: agent-object:coo
      - axis: Link
        row_ref: link-row:ccl0-closure-to-boundary
        movement: forward
        building_lifecycle:
          state: closed
          reason: CHECKER-COMMON-LIB-0 closed.
        target_ref: building-boundary:checker-common-lib-0-closed
        next_brick_instance_ref: building-boundary-checker-common-lib-0-closed
