plan_ref: building-plan:provider-native-projection-0-coo-sync-dogfood
owner_axis: Brick
building_id: provider-native-projection-0-0525
plan_shape: linear
selected_adapter_ref: adapter:local
proof_limits:
  - support evidence only
  - not source truth
  - not success judgment
  - not quality judgment
  - not Movement authority
not_proven:
  - Codex app reload behavior
  - Claude app reload behavior
  - provider quality
  - DEV/QA projection readiness
steps:
  - step_ref: provider-native-projection-design
    selected_adapter_ref: adapter:local
    rows:
      - axis: Brick
        row_ref: brick-row:provider-native-projection-design
        brick_work_ref: work:provider-native-projection-design
        brick_instance_ref: brick-provider-native-projection-design
        work_statement: Design PROVIDER-NATIVE-PROJECTION-0 around the admitted COO-SYNC-0 writer.
        comparison_rule: Observe projection boundary only; agent/ remains source.
        required_return_shape: projection_targets, source_owner, held_targets, proof_limits, not_proven
        source_facts:
          - AGENTS.md
          - support/docs/spec/brick-protocol-provider-native-projection-0-agent-resource-projection-0525.md
          - support/connection/coo_sync.py
      - axis: Agent
        row_ref: agent-row:provider-native-projection-design
        agent_object_ref: agent-object:coo
      - axis: Link
        row_ref: link-row:provider-native-projection-design-to-write
        movement: forward
        target_ref: brick-provider-native-projection-write
  - step_ref: provider-native-projection-write
    selected_adapter_ref: adapter:local
    rows:
      - axis: Brick
        row_ref: brick-row:provider-native-projection-write
        brick_work_ref: work:provider-native-projection-write
        brick_instance_ref: brick-provider-native-projection-write
        work_statement: Write exact local COO Codex and Claude projections using the admitted COO-SYNC-0 support writer.
        comparison_rule: Observe written target records and hashes only; do not call providers.
        required_return_shape: written, projection_status, proof_limits, not_proven
        source_facts:
          - support/connection/coo_sync.py
          - agent/objects/coo.yaml
          - agent/prompts/coo.md
          - agent/skills/building-coordination/SKILL.md
      - axis: Agent
        row_ref: agent-row:provider-native-projection-write
        agent_object_ref: agent-object:coo
      - axis: Link
        row_ref: link-row:provider-native-projection-write-to-qa
        movement: forward
        target_ref: brick-provider-native-projection-qa
  - step_ref: provider-native-projection-qa
    selected_adapter_ref: adapter:local
    rows:
      - axis: Brick
        row_ref: brick-row:provider-native-projection-qa
        brick_work_ref: work:provider-native-projection-qa
        brick_instance_ref: brick-provider-native-projection-qa
        work_statement: Verify provider-native projection files match rendered COO projections and DEV/QA remain held.
        comparison_rule: Observe projection hashes and checker output only.
        required_return_shape: checker_observations, projection_files, held_targets, proof_limits, not_proven
        source_facts:
          - support/checkers/check_coo_sync0_local_projection.py
          - support/checkers/check_provider_native_projection0.py
      - axis: Agent
        row_ref: agent-row:provider-native-projection-qa
        agent_object_ref: agent-object:qa
      - axis: Link
        row_ref: link-row:provider-native-projection-qa-to-closure
        movement: forward
        target_ref: brick-provider-native-projection-closure
  - step_ref: provider-native-projection-closure
    selected_adapter_ref: adapter:local
    rows:
      - axis: Brick
        row_ref: brick-row:provider-native-projection-closure
        brick_work_ref: work:provider-native-projection-closure
        brick_instance_ref: brick-provider-native-projection-closure
        work_statement: Close PROVIDER-NATIVE-PROJECTION-0 with observed projection evidence and next COO Movement.
        comparison_rule: Preserve observed evidence, narrow proof, not-proven, held, and next Movement only.
        required_return_shape: observed_evidence, narrowly_proven, not_proven, held, coo_movement
        source_facts:
          - support/docs/spec/brick-protocol-provider-native-projection-0-agent-resource-projection-0525.md
          - project/brick-protocol/buildings/provider-native-projection-0-0525/work/building-map.json
      - axis: Agent
        row_ref: agent-row:provider-native-projection-closure
        agent_object_ref: agent-object:coo
      - axis: Link
        row_ref: link-row:provider-native-projection-closure-to-boundary
        movement: forward
        building_lifecycle:
          state: closed
          reason: superseded closure Movement wording moved to Building lifecycle close state by MOVEMENT-BINARY-0
        target_ref: building-boundary:provider-native-projection-0-closed
