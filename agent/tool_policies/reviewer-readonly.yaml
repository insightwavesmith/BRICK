{
  "tool_policy_ref": "tool-policy:reviewer-readonly",
  "owner_axis": "Agent",
  "allowed_use": "read scoped files and run verification commands",
  "mutation_scope": "none unless an in-scope repair is separately assigned",
  "execution_opened": false,
  "forbidden_use": [
    "mutate files by default",
    "write outside a separately declared repair Brick",
    "git commit or git push",
    "choose Link Movement",
    "choose route target",
    "classify success, failure, quality, or approval"
  ],
  "proof_limits": [
    "Agent-axis tool policy only",
    "not a provider runtime",
    "not source truth",
    "not success judgment",
    "not quality judgment",
    "not Movement authority"
  ]
}
