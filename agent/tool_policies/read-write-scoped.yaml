{
  "tool_policy_ref": "tool-policy:read-write-scoped",
  "owner_axis": "Agent",
  "allowed_use": "read, source_write for implementation Bricks, and probe_write / verification_write for reviewer QA only through an admitted Brick write_scope and compatible write-capable adapter support",
  "semantic_capability_classes": [
    "read",
    "probe_write",
    "verification_write",
    "source_write",
    "artifact_write"
  ],
  "mutation_scope": "Brick-declared write_scope only; reviewer lanes may use it for disposable W1/temp/cache/test fixture/checker output/negative probe/generated probe output writes, not real repo source mutation",
  "execution_opened": true,
  "execution_opening_condition": "Effective write opens only when Brick write_scope, this read-write scoped Agent policy, compatible adapter mapping/technical capability, and support write observation are all present; not adapter identity alone",
  "native_grant": {
    "schema": "native-grant/v1",
    "capabilities": [
      "read",
      "write"
    ],
    "write_mode": "runtime_intersection",
    "proof_limits": [
      "native grant data only",
      "write projects only through runtime intersection with Brick write_scope NEED",
      "not provider runtime",
      "not source truth",
      "not success judgment",
      "not quality judgment",
      "not Movement authority"
    ]
  },
  "forbidden_use": [
    "write outside Brick-declared write_scope",
    "reviewer source_write: QA/Inspector/code-attack-qa/axis-attack-qa/evidence-integrity may not create, edit, or delete real repo source files",
    "write secret, token, credential, auth, or .git paths",
    "git commit or git push",
    "choose Link Movement",
    "create GateFact",
    "classify success, failure, quality, or approval"
  ],
  "proof_limits": [
    "Agent-axis tool policy only",
    "not a provider runtime",
    "not source truth",
    "not success judgment",
    "not quality judgment",
    "not Movement authority"
  ]
}
