# PIN-ESTATE RATCHET baseline (check_pin_estate_integrity.py discipline c).
#
# Counts the surviving history-doc pin estate across all checker profiles:
# every path_exists / path_absent item, text_contains / text_absent block,
# and json_required_paths block whose target lives under
# project/brick-protocol/status/kernel/, brick_protocol/support/docs/spec/, or archive/.
# Growth OR shrink of any count REDs until a NEW dated disposition entry is
# APPENDED here recording the new counts + who disposed it. This is the
# anti-silent-drift ledger for pins: W2 found 82 decorations precisely
# because pins had accumulated without a trace; this file makes every future
# estate change leave one.
#
# ANTI-REWRITE: structure_template_integrity.yaml pins the LATEST disposition
# entry's count lines + note as text_contains needles, so rewriting a recorded
# entry IN-PLACE REDs unless the profile pin is touched in the same change.
# Every appended disposition must extend that pin to the new latest entry.
# Tamper-EVIDENT only, not tamper-proof (see checker docstring).
#
# Support record only. Decides nothing: NOT source truth, NOT success
# judgment, NOT quality judgment, NOT Movement authority.
schema: pin-estate-baseline/v1
dispositions:
  - date: "2026-06-11"
    path_exists_blocks: 11
    text_contains_blocks: 11
    text_absent_blocks: 16
    text_needles: 123
    total_blocks: 38
    note: "W2 DOC-DECOUPLE 0611, ledger archive/PIN-RETIREMENT-LEDGER-0611.md, operator+Smith; estate = KEEP-LIVE CWC pins + 16 GUARD-ONLY anti-stale text_absent guards (15 museum 0526 sweeps + p7 anti-overclaim) + deferred 0528 orchestration-ledger pins"
  - date: "2026-06-11"
    path_exists_blocks: 11
    path_absent_blocks: 0
    text_contains_blocks: 11
    text_absent_blocks: 16
    json_required_paths_blocks: 0
    text_needles: 123
    json_required_keys: 0
    total_blocks: 38
    note: "codex-review tightening B 0611, operator; estate collector extended to count path_absent items and json_required_paths blocks on the history prefixes (collector blind spot closed; both kinds are 0 on today's tree, prior counts unchanged, keyless json_required_paths on a history doc now rejected as decorative)"
  - date: "2026-06-11"
    path_exists_blocks: 9
    path_absent_blocks: 0
    text_contains_blocks: 8
    text_absent_blocks: 16
    json_required_paths_blocks: 0
    text_needles: 110
    json_required_keys: 0
    total_blocks: 33
    note: "Phase-2 hold ratified by Smith 0611: B-DASH-WIRE event-delta dashboard supersedes PROJECT-ORCHESTRATION-LEDGER-0 Phase-2 review hold; 3 tc blocks + 2 path_exists retired, 2 docs archived; ledger archive/PIN-RETIREMENT-LEDGER-0611.md"
  - date: "2026-06-11"
    path_exists_blocks: 10
    path_absent_blocks: 0
    text_contains_blocks: 9
    text_absent_blocks: 16
    json_required_paths_blocks: 0
    text_needles: 113
    json_required_keys: 0
    total_blocks: 35
    note: "T-FLATTEN v3 0611, operator; route-request historical-compat sheet retired to archive/brick-templates/review/ (zero live brick refs; its recorded active profile was already folded into link_routing_behavioral), the surviving link_routing_behavioral path_exists + 3-needle texts pins FOLLOW the rehome into the archive prefix (+1 path_exists, +1 text_contains, +3 needles); no pin deleted"
  - date: "2026-06-11"
    path_exists_blocks: 10
    path_absent_blocks: 0
    text_contains_blocks: 9
    text_absent_blocks: 16
    json_required_paths_blocks: 0
    text_needles: 113
    json_required_keys: 0
    total_blocks: 35
    note: "SPLIT-PREP 0611, operator; post_d_surface_recompile_p7 profile retired per checker-split-map-0611.md D-1/D-2 (13 -> 12 profiles): 18 duplicate rows retired with both-side proof, 8 unique blocks folded verbatim into structure_template_integrity (ledger archive/PIN-RETIREMENT-LEDGER-0611.md, SPLIT-PREP 0611 section); the estate's single p7 block (text_absent anti-overclaim guard on the archived P7 record) rehomed profile-side only, so every count is unchanged"
  - date: "2026-06-11"
    path_exists_blocks: 9
    path_absent_blocks: 0
    text_contains_blocks: 8
    text_absent_blocks: 2
    json_required_paths_blocks: 0
    text_needles: 42
    json_required_keys: 0
    total_blocks: 19
    note: "REPO-SPLIT seed 0611, operator; product repo BRICK seeded per checker-split-map-0611.md: the archive/ museum pins moved to the history repo with their files (T-FLATTEN route-request sheet path_exists+3-needle text_contains retired product-side; 15 BAR-v2 0526 museum text_absent sweeps + 1 archived-P7 anti-overclaim guard moved with the archived docs), and the cascade_sweep 5-literal anti-stale sweep re-homed onto the two SHIPPED status/kernel history records (2 text_absent blocks, 10 needles); surviving estate = KEEP-LIVE CWC pins + the 2 re-homed guards"
  - date: "2026-06-11"
    path_exists_blocks: 0
    path_absent_blocks: 0
    text_contains_blocks: 0
    text_absent_blocks: 0
    json_required_paths_blocks: 0
    text_needles: 0
    json_required_keys: 0
    total_blocks: 0
    note: "CLEAN-YARD v3 0611, operator per Smith ruling (project-0 ships no standing inspection specimens): the entire surviving history-doc pin estate left WITH its subjects -- the 9 CWC path_exists pins + 8 CWC text_contains blocks (42 needles) retired when current-working-context.md (vessel #1 dogfood working context) moved to the frozen museum repo (law-text twins stay pinned on AGENTS.md and on the owning engine modules; CWC structure block superseded), and the 2 cascade_sweep anti-stale text_absent guards moved with the two 0530/0531 status/kernel history records; the estate's former properties are now EXECUTED cases over check-time engine-generated evidence (tier_a generation harness, intake_evidence_projection_case, link_route_evidence_case, native-dispatch posA fold, reporter inbox-packet fold); adversarial probe sampling generalized for an empty estate (six synthetic FIRE probes remain RED-first)"
  - date: "2026-07-08"
    path_exists_blocks: 2
    path_absent_blocks: 0
    text_contains_blocks: 2
    text_absent_blocks: 0
    json_required_paths_blocks: 0
    text_needles: 19
    json_required_keys: 0
    total_blocks: 4
    note: "Route V2 R0/R1 checker-first slice, Smith-approved direction recorded in GOAL 0708: re-grown status/kernel pin estate only for route-v2-sealed-materialization architecture and GOAL evidence; pins guard support evidence for sealed concern kinds, non-reroute verification_gap, gate/lifecycle-vs-Movement separation, and delta-QA fact fields; not source truth, success judgment, quality judgment, or Movement authority"
