# Public Projection Evidence

Brick Protocol support records live in this repository under
`brick_protocol/support/docs/spec/` and `project/brick-protocol/status/kernel/`.

The current Brick Engine docs-spec projection is reading support only:

```text
https://docs-spec.vercel.app/spec/three-axis-full-spec
```

That public page does not replace this repository's source records.

No root-level public projection/deploy surface is active in this repository.

The historical `architecture-review-site/` root has been removed from the
active repository shape. Historical records may still mention it as support
evidence only; those records do not recreate a docs site, Vercel deployment
surface, source truth, success judgment, or Movement authority.
