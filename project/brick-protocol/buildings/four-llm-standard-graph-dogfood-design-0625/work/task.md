BRICK COO declared P4 design Building: 4-LLM standard graph dogfood design.

Context already proven:
- P1 QA concern emission policy and Link policy alignment are committed.
- P2 checker/profile diet measurement and profile table are committed; actual taxonomy/diet implementation remains open.
- P3 Claude adapter/YAML control proof is committed: adapter:claude-local is write-capable in structure; effective write is controlled by Brick write_scope NEED + Agent tool-policy + observed-write adapter. Runtime Claude write proof remains not_proven.

Required design work (NO file edits, NO live provider runs, NO git mutation):
1. Design the smallest standard 4-LLM dogfood graph that uses:
   - Codex for implementation/work lane,
   - Claude for structure read / QA / cross-check lane,
   - Gemini for broad review / alternative sanity lane,
   - Fugu for deep boundary / axis-separation / design-attack lane.
2. Preserve BRICK 3-axis law:
   - Brick owns work contracts, return shapes, and Building Plan composition.
   - Agent owns performer identity/capability; adapters are brain connectors only.
   - Link owns Movement/reroute/fan-out/fan-in/gate sufficiency.
   - support records/walks/projects only; no source truth, quality, success, or Movement authority.
3. Make the graph HOLD-safe: fan-out may run in parallel only where HOLD/reroute/fan-in ordering is preserved; closure-synthesis alone should surface Link-facing transition_concern_evidence for hard fan-in QA if needed.
4. Include concrete node list, agent_object_ref, adapter_ref, model_ref candidate, write_scope need, return-shape intent, Link movement/target/fan-in policy, and evidence/ref handoff expectations.
5. Explicitly mark what can be run now vs what remains not_proven until live 4-provider execution.
6. Output a next Building candidate for actually running the graph (or a smaller read-only smoke) and the minimal preflight checks needed.

Return exact observed_evidence refs, narrowly_proven, not_proven, and next Movement candidate. Avoid --all and any long live-provider run.
