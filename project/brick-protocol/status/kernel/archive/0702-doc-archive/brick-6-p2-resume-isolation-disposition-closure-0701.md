# BRICK 6 P2 Resume Isolation / Explicit Disposition — Closure Evidence (0701)

Status: support evidence for phase:P2. Not source truth, not success/quality judgment, not Movement authority, not whole-goal completion.

## Objective context

P2 of the BRICK 6-surface audit repair goal = post-HOLD resume isolation, explicit disposition, and sensitive-path write commit block/mark. Implementation must run through official `build()` / `brick build` declared graph Buildings.

## Observed evidence

- P2 graph declared: `project/brick-protocol/status/kernel/GOAL/brick-6-p2-resume-isolation-disposition-0701a.json`.
- Official build root: `/Users/smith/.brick/project/brick-protocol/buildings/brick-6-p2-resume-isolation-disposition-0701a`.
- Build frontier: `frontier_kind=complete`.
- First closure carried Code QA `implementation_gap` for generic `resume_building_plan()` adapter_cwd enforcement. Link adopted a reroute to the work node (`raw:link-reroute:01`) rather than silently completing.
- Work attempt 2 repaired the gap and returned changed files in support/operator and support/checkers only.
- Final QA attempt 2 and closure attempt 2 returned `transition_concern_evidence=None`.
- Sandbox commit adopted into main as `dddcab1` from sandbox commit `074bf67`.
- Post-adopt current-main verification:
  - `python3 -m py_compile` for changed Python files: PASS.
  - `support/checkers/check_building_operator_driver0.py --repo .`: PASS.
  - `check_profile.py --profile support/checkers/profiles/building_operator_driver0.yaml --repo .`: PASS.
  - `git diff --check HEAD~1..HEAD`: PASS.
  - `check_profile.py --all`: PASS.

## Changed files adopted

- `support/operator/onboard.py`
- `support/operator/run.py`
- `support/operator/worktree_sandbox.py`
- `support/operator/write_observation.py`
- `support/checkers/check_building_operator_driver0.py`
- `support/checkers/check_bounded_agent_proposed_routing_loop0.py`
- `support/checkers/profiles/building_operator_driver0.yaml`

## Narrowly proven

- P2 explicit disposition action/author requirement is covered by focused checker evidence.
- P2 resume/post-HOLD adapter_cwd live-checkout isolation is covered by focused checker evidence and current-main `--all` evidence.
- Sensitive-path sandbox commit block/mark is covered by focused checker evidence and current-main `--all` evidence.
- HOLD remains lifecycle/frontier wording; Link Movement remains `forward` / `reroute` in the inspected evidence.
- The Building route exercised a real reroute from closure back to work and then closed after repair.

## Not proven

- Whole goal completion: P3-P8 remain.
- Real provider post-HOLD behavior and credential validity.
- Semantic completeness of all future provider/session/sensitive path naming patterns.
- Customer comprehension and public product readiness.
- Smith approval and push to origin/main.

## Next Movement candidate

Proceed to P3: Brick `return.yaml` truth + Link carry filtering, through its own declared graph Building and phase source audit docs. Keep P2 as support evidence only; do not mark the whole goal complete.
