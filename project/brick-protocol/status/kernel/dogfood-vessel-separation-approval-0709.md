# ⑩g Dogfood Vessel Separation Human-Gate Approval — 0709

Status: support evidence only. This records Smith's human-gate decision for the
`project/brick-protocol` dogfood vessel separation question. It does not move,
delete, rename, archive, migrate, implement, run a Building, choose Link
Movement, judge success/quality, or make source-truth claims.

## 0. Decision

Smith approved the COO-recommended option:

```text
Approved option: Option A — KEEP + clarify wording only
Approval text: "휴먼게이트 너의 의견대로한다."
Recorded interpretation:
  1. Keep `project/brick-protocol` as the active dogfood project vessel.
  2. Do not move, delete, archive, split, or migrate the active vessel now.
  3. Treat the path as project-local dogfood evidence/status, not product source.
  4. Allow wording/status clarification only where needed.
  5. Any future physical split remains a separate human-gated migration Building.
```

Source packet:

```text
project/brick-protocol/status/kernel/dogfood-vessel-separation-human-gate-0709.md
```

## 1. Approved current disposition

```text
phase: ⑩g dogfood vessel separation
state: closed_as_keep_and_clarify
approved_option: Option A
filesystem_migration: not_approved
move_delete_archive: forbidden_now
future_split_or_migration: requires_new_human_gate_and_declared_building
```

## 2. Live facts carried from the gate packet

Observed active vessel evidence recorded by the packet:

```text
project/brick-protocol/project.json exists
project/brick-protocol/README.md exists
project/brick-protocol/PROGRESS.md exists
project/brick-protocol/buildings/ immediate entries: 58
project/brick-protocol/status/kernel/ files: 94
project/brick-protocol/status/inbox/ event files: 929
```

Interpretation: this is an active declared dogfood vessel carrying current
GOAL/status/building evidence. Path confusion is not sufficient evidence for a
safe move/delete/archive.

## 3. Allowed and forbidden follow-up

Allowed now:

```text
- Continue ⑩f customer UX implementation Building from its declared request.
- Continue ⑥e/⑦ route-walker work only through its own approval/declaration path.
- Add clarifying wording in future docs/status when the vessel is mentioned.
```

Still forbidden without a later human-gated migration Building:

```text
- moving `project/brick-protocol`
- deleting or archiving `project/brick-protocol`
- splitting current status/buildings/inbox evidence into a new vessel
- rewriting historical evidence refs as cleanup
- treating project-local evidence/status as product source code
```

## 4. Three-axis attribution

```text
Brick:
  `project/brick-protocol` remains the active Brick Protocol dogfood project
  evidence vessel. The decision closes the ⑩g cleanup gate as KEEP, not as a
  migration implementation.

Agent:
  No Agent Object, skill source, adapter, provider, performer selection, or
  AgentFact shape is changed by this approval.

Link:
  This approval authors no Link Movement. The lifecycle gate closes ⑩g as KEEP;
  it does not choose forward/reroute for any implementation Building.

Support:
  The approval is a support/status record. It preserves evidence continuity and
  blocks unsafe support-file movement until a later declared migration Building.
```

## 5. Not proven

```text
- No vessel split is implemented.
- No migration target path is selected.
- No reference rewrite is proven.
- No deletion/archive is approved.
- Customer UX ⑩f implementation remains pending declared Building execution.
- Route-walker ⑥e/⑦ implementation remains pending its own declaration/proof.
```

## 6. Movement language

```text
gate_state: closed_as_keep_and_clarify
movement_candidate: none supplied by this approval
```
