# t7b replay 미러 수리 설계 최종본 (0706 — 설계 슬라이스 t7b-replaydesign-0706a attempt-4 수확)

출처: fable5 xhigh design ×4라운드(공격 QA fable5 + 증거 QA sonnet가 3회 반려 후 통과). 원본 = vessel step-output attempt-4.
Smith 0706 위임 하 COO 채택: D2=A방향(정합 미러링), D4=분할(픽스처 슬라이스 레인 발주 → walker 수술 COO-직접-언더-기계게이트), D5=자기잠금 가족② 분리 슬라이스.
이 문서는 설계 증거이며 source truth·성공/품질 판정·Movement 권한이 아니다.

===== design_summary =====
Repair design for the non-forward resume-replay rejection at walker_kernel.py:1746-1752, revised to close the carried D3 fixture-shape concern. D1: the mirror surface is larger than the 3 named points — beyond splice_after_current, node_landings, and the adoption record, replay integrity requires adoption_sequence_number parity (the 1719 pre-check increment must be reused, not re-incremented, for a mirrored reroute, and the existing forward mirror's unrolled increment is a measured structural skew hazard), attempt_number, reroute_ref/parent_reroute_ref threading, runtime mail re-read from the ledger, declared replay-scope items, cohort re-verify state (completed_fan_steps/cohort_skip_carry_forward/fan_in_cohort_records), reroute_insert_width, prior-disposition lifecycle re-stamping, and NO re-emission of disposition report events. Additionally the concern-path hold sites (1987/2024/2062/2097/2153) carry no previous-disposition check at all. D2: A (integrity mirroring) over B (seed disposition snapshot) — B still has to reproduce the frontier/landings state for the rerouted target to consume its recorded return, so B collapses into A plus a wide ResumeSeed/evidence schema change; C (fresh-only) remains the honest zero-engine-risk interim and is already the operational disposition (0705). D3: RESTORED per-fixture RED→GREEN contract for all four required fixtures (reroute×1, reroute×2, stop, mixed) with the exact engine rejection literal; the stop fixture is defined as a CURRENT stop disposition replaying past a PRIOR reroute (reachable at HEAD), and prior-stop-observation unreachability (walker_resume.py:198-201) is an ADDITIVE labeled probe, not a fixture replacement. D4: fixture/checker slice is lane-issuable under a narrow write_scope; the walker_kernel.py edit is COO-direct under the machine gate (RED→GREEN pair + isolated --all) with Smith return condition. D5: keep separate from 0702 self-lock family ② as an implementation slice; share the fixture corpus home.

===== proposed_changes =====
[0] {'deliverable': 'D1 — complete state-change inventory for replay-integrity mirroring (all walker_kernel.py cites)', 'items': ['1. node_landings[target] += 1 (1816 gate-seq / 2187 concern path)', "2. adoption_sequence_number: REUSE the 1719 prospective-hold increment for the mirrored reroute's reroute_ref instead of incrementing again at 1817/2188 — in the original resume the disposed hold's number was consumed by the adoption, so re-incrementing skews every later reroute_ref/parent_reroute_ref/hold identity (walker_hold.py:106-131 parity invariant)", '3. attempt_number = node_landings[target] (1818/2189)', '4. reroute_ref derivation (1820-1823/2191-2194) and parent_reroute_ref threading on spliced items', '5. frontier_driver.splice_after_current with cascade_depth+1, is_reroute_landing=True (1825-1834/2211-2218, 2272)', '6. reroute_records.append(build_reroute_adoption_record(...)) (1835-1864/2278-2314)', '7. CONCERN-PATH ONLY: runtime_mail re-read from ledger (2137-2150, 2201-2208), declared replay-scope items (2220-2232), cohort re-verify plan incl. completed_fan_steps additions, cohort_skip_carry_forward, fan_in_cohort_records (2240-2271), reroute_insert_width feeding the successor-splice offset (2273, 1890-1898)', '8. NON-changes the mirror must guarantee: no held_fan_steps.add, no hold record append, no _emit_disposition_applied_event re-emission (mode-1 only, 1624-1636)', '9. Prior-disposition lifecycle re-stamp on the prior held-source occurrence: the current re-walk rebuilds step_results from replayed returns and only stamps the CURRENT disposition (2353-2365), so prior resumed-lifecycle rows are not reproduced by any code read here — the mirror slice must re-stamp or the fixture must prove the row survives (runtime consequence unmeasured, see not_proven)', "10. Forward-mirror sequence skew: existing mirror (1753-1760) leaves the 1719 increment consumed while the original mode-1 forward (1651-1663) consumed none — a mixed chain's later identities can diverge (structural observation; runtime consequence goes to fixture F-M)", '11. Mirror SITES gap: the five concern-path hold constructions (1987, 2024, 2062, 2097, 2153) consult no previous disposition — a previously-disposed concern-path hold silently re-holds on replay (no ValueError). Minimum slice: convert silent re-hold into a loud, literal-carrying rejection; full slice: mirror there too', "12. Interactions that need NO new state: prior raise (budget recovery, walker_resume.py:242-262 + kernel 1129-1132), prior stop (unreachable upstream, walker_resume.py:198-201), expected_replay_counts already covers the mirrored target landing's recorded return consumption (walker_resume_seed.py:155-201)"]}
[1] {'A_change_points': 'walker_kernel.py:1746-1760 (branch replacement: prev reroute → set human_disposition_reroute_target from previous_disposition.pending_target_ref, mutate gate_sequence_decision as at 1670-1683, fall into 1776 with sequence-number reuse per D1.2; prev forward → keep, plus sequence rollback per D1.10); optional shared-helper extraction of 1816-1864/2185-2314; concern-path sites per D1.11; risks: silent mis-mirroring (duplicate splice, landing-count error, adoption-record mismatch — the exact FIX-2 stale-identity class, walker_hold.py:109-120), two adoption flavors to reproduce, lifecycle re-stamp gap.', 'B_change_points': "walker_resume_seed.py:61-115 (ResumeSeed schema: per-hold disposition history + post-disposition frontier/landings snapshot), walker_resume.py:386-410 (seed build), kernel consumption sites; risks: breaks the delegate-to-forward-loop axiom, duplicates adoption bookkeeping in a second representation, wide evidence-schema surface; decisive: B still must land the rerouted target in the frontier with correct cascade/parent refs so its recorded return replays — i.e., B contains A's hard part plus schema churn.", 'C_honesty': 'C (fresh re-issue for disposed-history buildings) is currently in force (source_facts 0705 COO disposition) and requires no engine touch; adopt A only with the D3 gate pair admitted RED first.', 'deliverable': 'D2 — A vs B judgment', 'judgment': 'A (integrity mirroring) is the design-correct repair; B is rejected; C (fresh-only) remains the honest interim and is not beaten on risk — A is justified on design integrity (the resume axiom walker_resume_seed.py:23-39 promises full forward fidelity across generations), C wins on zero engine risk at re-run cost.'}
[2] {'deliverable': 'D3 — non-forward replay fixture/gate pair spec (RESTORES the original per-fixture RED→GREEN contract; closes transition-concern:task-statement-cf0c9857bd98-node:d3-fixture-shape)', 'spec': 'New checker support/checkers/check_resume_replay_disposition_mirror.py; each fixture builds a synthetic building ledger (birth-certificate graph plan, recorded agent returns, step-output ledger, raw/link.jsonl disposition rows) and drives resume_building_plan with a deterministic replay_step (admitted use per run.py:716-719). Four REQUIRED fixtures, each an independent RED→GREEN pair: [F-R1 reroute×1] prior reroute at H1, current disposition at H2 — RED at HEAD: ValueError whose message starts with the engine literal \'resume replay encountered an already-disposed recorded HOLD for \' and ends with "unsupported prior disposition \'reroute\'" (walker_kernel.py:1749-1751); GREEN: resume completes, mirrored reroute_ref string-equal to the prior generation\'s recorded reroute-adoption ref, node_landings equal to persisted evidence, exactly one new resume observation appended. [F-R2 reroute×2] two prior reroute dispositions (the measured 0705a/0705b shape) — same RED literal; GREEN adds sequence parity across both mirrored adoptions. [F-S stop] prior reroute at H1 + CURRENT stop at H2 — RED: same literal raised at H1 before the stop can apply; GREEN: replay passes H1 mirror, stop applies at H2, building lifecycle closed (state \'closed\', reason \'ended-by-disposition\', walker_resume_seed.py:296-303). [F-M mixed] prior forward at H1 + prior reroute at H2 + current disposition at H3 — RED: same literal at H2; GREEN: completion plus forward-mirror sequence-parity assertion (catches D1.10). ADDITIVE probes (labeled probes, not fixture substitutes): [P-1] prior-stop chain refused upstream with literal \'dynamic Building already has an applied resume disposition\' (walker_resume.py:200); [P-2] previously-disposed concern-path hold currently re-holds silently (documents D1.11); [P-3] divergence guard literal intact (\'resume divergence: the seeded walk completed WITHOUT applying the\', walker_kernel.py:2346).'}
[3] {'deliverable': 'D4 — construction plan', 'judgment': "Split. Slice 1 (lane-issuable): author the checker + fixtures RED-pinned, write_scope = ['support/checkers/check_resume_replay_disposition_mirror.py', 'support/checkers/module_registry.yaml'] (registry row is a declarative append mirroring module_registry.yaml:230-238; note write_scope glob grammar — enumerate files, a bare directory form excludes children). Rationale: fixtures cannot damage the engine, and checker-first gives the RED baseline before any walker edit. Slice 2 (COO-direct under machine gate, NOT lane-issuable): the walker_kernel.py mirror edit, gated on the admitted RED→GREEN pair flipping plus an isolated full checker run (--all), with Smith return condition — rationale: source_facts declares walker resume integrity the heart under the 불가침 boundary, and a wrong mirror fails silently in the FIX-2 stale-identity class (walker_hold.py:109-120), which exceeds a worker lane's blast radius. The deferred admission-registry question resolves as: registry edit rides Slice 1 (lane) because it admits only the checker, not the engine change."}
[4] {'deliverable': 'D5 — 0702 self-lock family ② relation', 'judgment': "SEPARATE implementation slices, SHARED fixture home. The replay-mirror repair surface measured here is the mode-2 branch + adoption bookkeeping; coupling family ②'s repair into the same gated walker edit widens the highest-risk surface for no shared code benefit measured in this read scope. The new checker module is the natural later host for family-② fixtures (one registry row, additive profiles). Family-② specifics were not readable in this task's declared scope — see not_proven."}

===== invariants =====
[0] I1 generation parity: a resumed walk replaying prior generations reproduces the identical adoption_sequence_number / reroute_ref / parent_reroute_ref / node_landings prefix (designed property, walker_hold.py:106-131)
[1] I2 no re-authoring: a mirrored disposition emits no new disposition event, no new hold record, no new lifecycle authorship — existing_resume_observations preserved verbatim + exactly one CURRENT observation appended (walker_kernel.py:2352-2372)
[2] I3 no provider calls at or before the recorded frontier (walker_resume_seed.py:169-195 fail-closed)
[3] I4 budget conservation: a mirrored reroute consumes exactly one landing from the same shared target budget as the original adoption; totals equal persisted evidence (walker_kernel.py:2386-2387)
[4] I5 stop is terminal: a prior-stop observation is never walked past; closed buildings stay refused upstream (walker_resume.py:198-201)
[5] I6 fail loud, never silent: any prior disposition the mirror cannot faithfully reproduce raises with a literal string; no silent re-hold and no silent walk-on

===== checker_or_verifier_plan =====
New behavioral checker support/checkers/check_resume_replay_disposition_mirror.py (pattern: check_resume_disposition_surface.py:1-25), registered in module_registry.yaml (row shape at 230-238). Profiles: F-R1, F-R2, F-S, F-M as four independent RED→GREEN pairs pinned to the engine literals — RED prefix 'resume replay encountered an already-disposed recorded HOLD for ' (walker_kernel.py:1749-1751); probes P-1 ('dynamic Building already has an applied resume disposition', walker_resume.py:200), P-2 (silent concern-path re-hold), P-3 (divergence literal, walker_kernel.py:2346). GREEN assertions per fixture: completion/closure state, reroute_ref string parity with prior-generation recorded adoption records, node_landings/node_budget equality with persisted evidence, observation monotonicity (I2), zero live provider calls via a counting replay_step (I3), and for F-M the sequence-parity assertion (D1.2/D1.10). Admission order: checker+fixtures land RED-pinned on a pinned baseline BEFORE any walker edit; the walker edit's gate = the four pairs flip to GREEN + probes hold + isolated full checker run (--all) exits 0 with no regression. This plan verifies mechanics only; sufficiency stays with the Link gate and quality with the human.

===== edge_cases =====
[0] Forward-mirror sequence consumption at walker_kernel.py:1719 with no rollback at 1760 — later hold identities may diverge across generations (F-M asserts)
[1] Prior disposition on a CONCERN-path hold (pause/ambiguous/unbudgeted/exhausted/broken-mail): currently silent re-hold, not the ValueError (P-2)
[2] Mirrored reroute whose original adoption ran the concern path: runtime mail must re-resolve from the (rewritten) ledger at replay time (walker_kernel.py:2142-2150 broken-mail hold if it cannot)
[3] Prior raise + later reroute in one chain: recovered pre-bumped budgets (walker_resume.py:242-262) suppress the raise's hold rebuild — mirror must not double-count that landing
[4] Fan graphs: reroute_insert_width offset for successor splice after a mirrored adoption (walker_kernel.py:1890-1898, 2273); mirrored holds must not enter held_fan_steps
[5] Multiple disposition-shaped rows for the same hold identity (FIX-2 stale-row class): _resume_observation_for_hold returns the FIRST match (walker_resume_seed.py:228-240)
[6] At-time gate record for the disposed occurrence replays 'hold' even though the original mode-1 run mutated the decision to reroute before recording downstream — mirror must key off the observation, not the gate record
[7] Budget exhausted at mirror time for a previously-adopted target (should be impossible under I1; assert loudly rather than hold)

===== not_proven =====
[0] brain surface behavior
[1] credential validity
[2] tool or hook execution
[3] runtime or scheduler behavior
[4] quality of returned work
[5] Runtime confirmation of the forward-mirror adoption_sequence_number skew consequence (D1.10) — structural read only; F-M must measure it
[6] Runtime confirmation that prior resumed-lifecycle rows are lost on re-walk (D1.9) — inferred from stamp sites read; fixture must measure
[7] Whether the measured 0705a/0705b blockers' original holds were gate-sequence-branch or concern-path holds (their ledgers are not in this worktree); the mirror design covers both flavors regardless
[8] Behavior of gate-record replay interaction at the disposed occurrence (edge case) — not executed here
[9] 0702 self-lock family ② repair surface — its documents were outside this task's declared read scope; the D5 separate-slice judgment rests on measured surface size here plus the packet-carried family description
[10] The '44 profiles pass' clean --all baseline is a packet-carried claim, not re-measured by this lane
[11] Semantic correctness of the proposed mirror after implementation; implementation readiness; fixture authoring/admission feasibility as specified
[12] Prior design attempt 3 / evidence-integrity attempt 3 bodies (files absent in this worktree; known only via the packet-carried transition-concern summary)
[13] This return is design evidence only — not source truth, not success or quality judgment, not Movement authority

===== observed_evidence =====
[0] Defect site measured at HEAD: support/operator/walker_kernel.py:1746-1752 — inside the gate-sequence 'hold' branch, `_resume_observation_for_hold` returns a prior disposition observation and any `disposition_action != "forward"` raises ValueError("resume replay encountered an already-disposed recorded HOLD for {step_ref!r} with unsupported prior disposition {action!r}"). Forward-only mirror at walker_kernel.py:1753-1760 (splice declared successors + continue).
[1] The prospective hold consumes `adoption_sequence_number += 1` at walker_kernel.py:1719 BEFORE the previous_disposition check at 1742-1746; the forward mirror continues at 1760 without rolling that increment back.
[2] Live (mode-1) disposition application at the current resume's held occurrence: walker_kernel.py:1614-1663 (guard 1614-1620, stop 1637-1650 via _stamp_resumed_lifecycle_on_held_source + break, forward 1651-1663 splice+continue with NO sequence increment, reroute 1664-1683 sets human_disposition_reroute_target and mutates gate_sequence_decision hold→reroute so the existing adoption machinery runs).
[3] Gate-sequence reroute adoption state changes: walker_kernel.py:1816 node_landings[target]+=1, 1817 adoption_sequence_number+=1, 1818 attempt_number, 1820-1823 reroute_ref embeds building_id+adoption_sequence_number+target, 1825-1834 frontier_driver.splice_after_current (cascade_depth+1, parent_reroute_ref, is_reroute_landing), 1835-1864 build_reroute_adoption_record appended to reroute_records.
[4] Concern-path reroute adoption is a SECOND, richer path: walker_kernel.py:1943-1949 (human target → _RerouteTargetClassification, adopted_by=link-policy:human-disposition), 2137-2150 runtime mail read back FROM the ledger, 2187-2188 landings+sequence, 2201-2232 runtime_mail + target landing + declared replay scope items, 2240-2271 cohort re-verify plan (completed_fan_steps.add for vouched skips, cohort_skip_carry_forward, fan_in_cohort_records), 2272 splice_after_current, 2273 reroute_insert_width (consumed by successor splice offset at 1890-1898), 2278-2314 adoption record with cohort_replay_segment_refs.
[5] Concern-path hold construction sites carry NO previous-disposition check at all (grep `_resume_observation_for_hold` in walker_kernel.py matches only line 1742): ambiguous/none 1987-2017, gate pause 2024-2054, unbudgeted 2062-2092, budget exhausted 2097-2127, broken mail 2153-2183 — each appends a hold and breaks.
[6] Budget/landings initialization: walker_kernel.py:1123 (node_budget from plan), 1129-1132 (current-resume budget_delta applied; raise verified byte-identical per comment 1125-1128), 1134 (node_landings zeroed every walk generation).
[7] Seed construction and gates: support/operator/walker_resume.py:198-201 (a non-held building with existing resume observations is refused: "dynamic Building already has an applied resume disposition" — a prior-stop observation therefore cannot reach the kernel check), 216-223 (birth-certificate required), 242-262 (node budget recovery from persisted evidence — this is how prior-raise chains self-heal), 296-333 (replay FIFOs + fail-closed frontier consistency), 338-346 (budget_delta only for the CURRENT raise).
[8] Prior-disposition matching data: support/operator/walker_resume_seed.py:221-240 (_resume_observation_for_hold matches hold identity string against paused_at_ref/resumed_from/provenance of existing_resume_observations) and 378-399 (the persisted observation carries disposition_action, pending_target_ref, budget_increment — the data a mirror needs).
[9] Hold identity embeds adoption_sequence_number by design: support/operator/walker_hold.py:106-131 docstring states the identity is reroute_ref (embedding adoption_sequence_number, which resets each walk generation) discriminated by source_step_ref+cascade_depth, and that "the first N-1 sequence increments of a resumed walk ARE the prior walk's adoptions, replayed" — sequence parity across generations is a designed invariant.
[10] Post-loop: walker_kernel.py:2344-2351 divergence guard literal ("resume divergence: the seeded walk completed WITHOUT applying the ..."), 2352-2372 existing observations preserved + exactly one new observation appended; 2353-2365 lifecycle stamp for the CURRENT raise/forward/reroute only — no re-stamp of PRIOR dispositions' held-source lifecycle rows on a re-walk.
[11] Deterministic-replay testing is an admitted use: support/operator/run.py:716-719 ("The generic resume API remains usable for deterministic replay tests"); resume verb at run.py:622, onboard approval path calls resume_building_plan at support/operator/onboard.py:3571-3581.
[12] Checker home pattern: support/checkers/check_resume_disposition_surface.py:1-25 (standalone behavioral checker, repo-root sys.path bootstrap) and its registry row support/checkers/module_registry.yaml:230-238.
[13] brick/templates/reroute-defaults.yaml:3 default_node_reroute_budget: 5 — sufficient for fixture graphs; no template change needed.
[14] Prior-attempt artifacts are NOT present in this worktree: Glob for work/step-outputs/task-statement-cf0c9857bd98-node-*attempt-3/step-output.json and project/brick-protocol/buildings/t7b-replaydesign-0706a/** returned no files; prior-attempt content is known here only via the packet-carried transition-concern summary.

